<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="it.appspice.sampleproject">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:icon="@drawable/ic_launcher"
        android:theme="@style/AppTheme">
        <meta-data
            android:name="APP_SPICE_ID"
            android:value="b64c745b4c2d95b9479d" />
        <meta-data
            android:name="APP_SPICE_APP_ID"
            android:value="5423f08150b6041b34a8a2ad" />

        <activity android:name="it.appspice.sampleproject.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name="it.appspice.sampleproject.SecondActivity" />

        <service
            android:name="it.appspice.android.services.InstalledAppsService"
            android:exported="true"
            android:process="it.appspice.InstalledAppsService" />
    </application>

</manifest>
